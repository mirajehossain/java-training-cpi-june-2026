# Phonebook Management System

First run:

```bash
mysql -u root -p < database.sql
```

Then run:

```bash
mvn clean compile
mvn exec:java
```

Credential:

```text
username: admin
password: 1234
```
